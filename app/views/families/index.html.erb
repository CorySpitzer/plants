<h1>Families</h1>

<%= link_to 'Add a New Family', new_family_path %>

<% @families.each do |family| %>
  <ul>
    <li>
      <%= link_to family.name, family_path(family.id) %>
    </li>
  </ul>
<% end %>
